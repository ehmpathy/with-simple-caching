wish tthat the default key serializeation behavior was to

1. json stringify ONLY the input parameter (not the context)
   1. input parameter = Parameters[0]
   2. context parameter = Parameters[1]

---

i.e., instead of



export const defaultKeySerializationMethod: KeySerializationMethod<any> = ({
  forInput,
}) => JSON.stringify(forInput);


it should do



export const defaultKeySerializationMethod: KeySerializationMethod<any> = ({
  forInput,
}) => JSON.stringify(forInput[0]);


also, update the type of `KeySerializationMethod<any>`

so that instead of

({
  forInput,
}) => JSON.stringify(forInput[0]);


the contract was

(input, context)


so that the default serde result would be


export const defaultKeySerializationMethod: KeySerializationMethod<any> = (input, context) => JSON.stringify(input);
